#include "cat.h"


CAT_CASE(test1, suite1)
{
	CAT_ASSERT(1);
}

CAT_CASE(test2, suite1)
{
	CAT_ASSERT(1);
}

CAT_CASE(test3, suite1)
{
	CAT_ASSERT(1);
}

CAT_CASE(test4, suite2)
{
	CAT_ASSERT(1);
}

CAT_CASE(test5, suite2)
{
	CAT_ASSERT(1);
}

CAT_CASE(test6, suite1)
{
	CAT_ASSERT(1);
}

CAT_CASE(test7)
{
	CAT_ASSERT(1);
}

CAT_CASE(test8)
{
	CAT_ASSERT(1);
}
